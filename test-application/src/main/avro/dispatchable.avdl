@namespace("io.github.dfauth.embedded.kafka.dispatchable")

protocol DispatchableEventProtocol {

  import idl "user.avdl";

  record UpdateEvent {
    io.github.dfauth.embedded.kafka.test.User payload;
  }

  record DeleteEvent {
  }
}