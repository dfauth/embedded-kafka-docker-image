@namespace("io.github.dfauth.embedded.kafka.test")

protocol UserProtocol {

  record User {
    long id;
    string userId;
    @logicalType("io.github.dfauth.embedded.kafka.test.FavouriteColour") int favoriteColor;
  }
}