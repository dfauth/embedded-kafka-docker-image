@namespace("io.github.dfauth.embedded.kafka.image.test")

protocol UserProtocol {

  record User {
    long id;
    string userId;
    @logicalType("io.github.dfauth.embedded.kafka.image.FavouriteColour") int favoriteColor;
  }
}